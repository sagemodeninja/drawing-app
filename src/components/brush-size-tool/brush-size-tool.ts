import { CustomComponent, customComponent } from '@sagemodeninja/custom-component';

@customComponent('brush-size-picker')
export class BrushSizeTool extends CustomComponent {
    static styles = `
    `

    public render() {
        return ``;
    }
}